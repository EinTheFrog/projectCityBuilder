package core;

import logic.BuildingTypes;

public class HouseCore extends AbstractBuilding {

    public HouseCore(double x, double y, int width, int length, int scale, FieldCore field, BuildingTypes type) {
        super(x, y, width, length, scale, field, type);
    }
    
}
